// Placeholder source file, seeded as part of SafeShell's demo project
// (see ../README.md). Not compiled or executed by anything in SafeShell —
// its only purpose is to be a real, readable file for `cat`/`ls` to show
// during a session, and a real target for demo operations like a
// recursive delete-and-undo.

fn main() {
    println!("hello from the demo project");
}
